<h1>All Events</h1>

<div class="events">
  <h2 class="past-heading">Past Events: </h2>
  <ul class="past-events">
    <% @events.past.each do |event| %>
      <li class="event">
        <%= link_to event.name, event, class: 'title' %>
      </li>
    <% end %>
  </ul>

  <h2 class="future-heading">Future Events: </h2>
  <ul class="future-events">
    <% @events.future.each do |event| %>
      <li class="event">
        <%= link_to event.name, event, class: 'title' %>
      </li>
    <% end %>
  </ul>
</div>